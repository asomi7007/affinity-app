# Affinity Diagram Frontend

React + Vite + TypeScript 기반 초기 UI.

## 실행
```bash
npm install
npm run dev
```

기본 WebSocket 엔드포인트: `ws://localhost:8000/ws/board/dev-board`
環境 변수: `.env` 파일에 `VITE_API_BASE=http://localhost:8000` 설정 가능.
